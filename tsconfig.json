{
    // Solution-style root: no files of its own, just references. `tsc --build`
    // walks these in dependency order. Each package is added here as it lands,
    // so the build stays green at every commit.
    "files": [],
    "references": [
        { "path": "./packages/mcp-core" },
        { "path": "./packages/policy-engine" },
        { "path": "./packages/audit" },
        { "path": "./packages/risk-engine" },
        { "path": "./packages/approvals" },
        { "path": "./apps/gateway" }
    ]
}
